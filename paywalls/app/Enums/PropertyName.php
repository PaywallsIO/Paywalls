<?php

namespace App\Enums;

final class PropertyName
{
    const appVersion = '$app_version';

    const initialAppVersion = '$initial_app_version';

    const appBuildNumber = '$app_build_number';

    const initialAppBuildNumber = '$initial_app_build_number';

    const appName = '$app_name';

    const initialAppName = '$initial_app_name';

    const appNamespace = '$app_namespace';

    const initialAppNamespace = '$initial_app_namespace';

    const osVersion = '$os_version';

    const initialOsVersion = '$initial_os_version';

    const libVersion = '$lib_version';

    const initialLibVersion = '$initial_lib_version';

    const deviceType = '$device_type';

    const initialDeviceType = '$initial_device_type';

    const os = '$os';

    const initialOs = '$initial_os';

    const deviceModel = '$device_model';

    const deviceName = '$device_name';

    const initialDeviceModel = '$initial_device_model';

    const screenWidth = '$screen_width';

    const screenHeight = '$screen_height';

    const manufacturer = '$manufacturer';

    const networkWifi = '$network_wifi';

    const networkCellular = '$network_cellular';

    const paywallId = '$paywall_id';

    const sessionDurationSeconds = '$session_duration_seconds';
}
